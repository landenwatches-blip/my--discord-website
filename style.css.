/* General body styling */
body {
  background-color: #0f0f0f;
  color: #fff;
  font-family: 'Arial', sans-serif;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  margin: 0;
  flex-direction: column;
}

/* Neon glowing main heading */
.neon {
  font-size: 3em;
  color: #39ff14;
  text-shadow:
    0 0 5px #39ff14,
    0 0 10px #39ff14,
    0 0 20px #39ff14,
    0 0 40px #39ff14;
  animation: flicker 1.5s infinite alternate;
}

/* Flicker effect for neon text */
@keyframes flicker {
  0% { text-shadow: 0 0 5px #39ff14, 0 0 10px #39ff14; }
  50% { text-shadow: 0 0 10px #39ff14, 0 0 20px #39ff14; }
  100% { text-shadow: 0 0 5px #39ff14, 0 0 15px #39ff14; }
}

/* Glow paragraph text */
.glow-text {
  color: #39ff14;
  text-shadow: 0 0 5px #39ff14, 0 0 10px #39ff14;
  margin-bottom: 20px;
  text-align: center;
}

/* Buttons styling */
button {
  margin: 10px;
  padding: 10px 20px;
  font-size: 1.2em;
  border: none;
  border-radius: 5px;
  background-color: #111;
  color: #39ff14;
  box-shadow: 0 0 10px #39ff14;
  cursor: pointer;
  transition: 0.2s;
}

button:hover {
  box-shadow: 0 0 20px #39ff14, 0 0 30px #39ff14;
}
